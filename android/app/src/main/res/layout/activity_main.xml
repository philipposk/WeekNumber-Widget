<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center">

        <!-- Week Number Display -->
        <TextView
            android:id="@+id/week_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Week"
            android:textSize="18sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:layout_marginTop="48dp"
            android:alpha="0.7" />

        <TextView
            android:id="@+id/week_number_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="46"
            android:textSize="96sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface"
            android:layout_marginTop="8dp"
            android:letterSpacing="0.02" />

        <!-- This week info card -->
        <LinearLayout
            android:id="@+id/this_week_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:layout_marginTop="32dp"
            android:background="?attr/colorSurfaceVariant"
            android:backgroundTint="?attr/colorSurfaceVariant">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="THIS WEEK"
                android:textSize="12sp"
                android:letterSpacing="0.08"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurfaceVariant" />

            <TextView
                android:id="@+id/week_dates_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Jun 9 – Jun 15"
                android:textSize="15sp"
                android:textColor="?attr/colorOnSurface"
                android:layout_marginTop="10dp" />

            <TextView
                android:id="@+id/days_left_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="200 days left in the year"
                android:textSize="15sp"
                android:textColor="?attr/colorOnSurface"
                android:layout_marginTop="6dp" />

            <ProgressBar
                android:id="@+id/year_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:max="100"
                android:progress="45" />

            <TextView
                android:id="@+id/year_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="45% of the year"
                android:textSize="13sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginTop="6dp" />
        </LinearLayout>

        <!-- App Description -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Display the current week number on your home screen"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:gravity="center"
            android:layout_marginTop="32dp"
            android:alpha="0.8"
            android:lineSpacingMultiplier="1.4" />

        <!-- Expandable Instructions -->
        <LinearLayout
            android:id="@+id/instructions_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="16dp"
            android:layout_marginTop="32dp"
            android:background="?attr/colorSurfaceVariant"
            android:backgroundTint="?attr/colorSurfaceVariant"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground">

            <TextView
                android:id="@+id/instructions_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="To add widgets to your home and lock screens"
                android:textSize="15sp"
                android:textColor="@color/accent_blue"
                android:textStyle="normal" />

            <TextView
                android:id="@+id/instructions_arrow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="▼"
                android:textSize="12sp"
                android:textColor="@color/accent_blue"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <TextView
            android:id="@+id/instructions_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_instructions"
            android:textSize="13sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:padding="16dp"
            android:background="?attr/colorSurfaceVariant"
            android:backgroundTint="?attr/colorSurfaceVariant"
            android:visibility="gone"
            android:alpha="0.9"
            android:lineSpacingMultiplier="1.5" />

        <!-- Configuration Button -->
        <Button
            android:id="@+id/configure_widgets_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Configuration"
            android:textSize="16sp"
            android:textColor="?attr/colorOnSurface"
            android:backgroundTint="?attr/colorSurfaceVariant"
            android:layout_marginTop="32dp"
            android:alpha="0.7"
            android:elevation="0dp" />

    </LinearLayout>
</ScrollView>
